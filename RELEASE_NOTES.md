### 설치 방법
1. 아래 Assets에서 `edunote_*.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. (설치 불필요)
3. 설정에서 Gemini API 키를 입력하면 바로 사용 가능합니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com) 에서 무료로 발급받을 수 있습니다.

---

## 🐛 v1.6.1 버그 수정

### API 키 테스트 오류 메시지 상세화
- 이전: 대부분의 오류가 "알 수 없는 오류" 하나로만 표시
- 이후: 잘못된 키 / 네트워크 오류 / 권한 거부(403) / 쿼터 초과 / 모델 미지원 등 원인별 상세 안내
- 최후 폴백에서도 실제 오류 메시지와 모델별 결과를 그대로 표시

### 상위 모델 자동 재시도 개선
- 403 권한 오류로 차단된 모델이 **1시간 후 자동 해제**되어 상위 모델(gemini-2.5-pro 등) 재시도
- 이전: 앱을 재시작하거나 API 키를 변경해야만 상위 모델 재시도 가능

### 설정 화면 빈 화면 버그 수정
- API 키 설정해 주세요 클릭 시 빈 화면이 나오던 문제 수정

---

## ✨ v1.6.0 업데이트 내역

### API 키 활성화 팝업 알림 (신규)
- API 키 저장 성공 시 "API 키 활성화 완료!" 팝업이 표시되고 **확인** 버튼으로 닫기 가능

### 사이드바 API 키 상태 즉시 반영
- 앱 실행 시 저장된 API 키가 있으면 사이드바가 즉시 녹색("API 키 활성화 완료!")으로 표시
- 이전: 앱을 껐다 켜야 상태가 바뀌는 버그 수정

### API 키 검증 속도 개선
- 키 유효성 검사 시 4개 모델을 순서대로 시도하던 방식(최대 48초)에서 **동시 요청 방식(최대 10초)**으로 변경

### 효과음 안정성 개선
- 공유 `AudioContext` 방식으로 변경하여 생성 완료 효과음이 더 안정적으로 재생

### AI 요청 타임아웃 개선
- 타임아웃 에러 메시지를 더 명확하게 표시

---

## ✨ v1.5.2 버그 수정

### 생성 완료 효과음 재수정
- Electron 오디오 차단 정책(autoplay policy)을 데스크톱 앱에 맞게 해제하여 효과음이 확실히 재생되도록 수정

---

## ✨ v1.5.1 버그 수정

### 중단 버튼 즉시 반응
- 중단 버튼 클릭 즉시 진행바가 사라지도록 개선 (이전: 현재 AI 호출이 끝날 때까지 진행바 유지)

### 생성 완료 효과음 수정
- Electron 환경에서 효과음이 들리지 않던 문제 해결 (`AudioContext` 일시 정지 상태 자동 해제)

### 나만의 자료실 — 제목 자동 가져오기
- YouTube URL: "정보 가져오기" 클릭 시 실제 영상 제목을 자동으로 가져와 입력 (기존: "YouTube 영상" 고정 텍스트)
- 웹페이지 URL: 기존과 동일하게 페이지 제목 자동 입력

---

## ✨ v1.5.0 업데이트 내역

### 생성 히스토리 (신규)
- 행동특성·세특·창체·스포츠 생성기에서 **학생별 최근 3개 결과를 자동 저장**
- 재생성 후 결과 화면에 **"이전 기록 (N)"** 버튼 표시 → 클릭 시 날짜·내용 미리보기
- **"복원"** 버튼으로 이전 결과를 현재 텍스트박스에 즉시 되돌리기

### 전체 복사 버튼 (신규)
- 교과 세특, 창체 특기사항, 학교스포츠클럽 결과 화면 상단에 **"전체 복사"** 버튼 추가
- 클릭 한 번으로 모든 학생 결과를 `[이름]\n내용` 형식으로 클립보드에 일괄 복사

### API 키 활성화 표시 개선
- 키 등록 여부가 아닌, **실제 AI 호출 성공 여부**를 기준으로 사이드바 배너 색상 전환
- 설정에서 키 저장·폴링·수동 확인 시 AI 호출 성공하면 즉시 초록색 배너로 전환

### 생성 중단 즉시 반영
- AbortController 기반으로 개선 — AI 호출 **도중에도** 중단 버튼 누르면 즉시 멈춤
- 기존 방식(다음 학생 전환 전 체크)보다 훨씬 빠른 중단

### 생성 완료 효과음
- 모든 생성기에서 일괄 생성 완료 시 기분 좋은 아르페지오 효과음 재생
- API 키 활성화 확인 성공 시에도 동일 효과음

### 다크모드 일관성
- 설정 화면, 수업자료 AI, 학생 메모보드, 교무행정 AI 챗봇에 다크모드 적용
- 사이드바 하단 다크/라이트 토글 버튼 가시성 개선

### UX 개선
- **학생 메모 전체 삭제**: 삭제 전 confirm 다이얼로그 추가 (실수 방지)
- **나만의 자료실**: 주제가 없을 때 분류 드롭다운에 "+ 주제 만들기" 버튼 안내
- **교무행정AI 챗봇**: 메뉴 이름 변경 (교무AI챗봇 → 교무행정AI 챗봇)

### 버그 수정
- Gemini 모델 전체 차단 시 null 오류 발생하던 문제 수정
- 나만의 자료실 분류 드롭다운이 카드에 가려지던 문제 수정

---

## 이전 버전 요약

| 버전 | 주요 변경 |
|------|----------|
| v1.4.0 | API 키 활성화 자동 감지·토스트 알림, 생성 중단 버튼 |
| v1.3.3 | 나만의 자료실 분류 즉시 변경, API 키 상태 시각화 |
| v1.3.2 | API 키 검증 로직 개선 (모든 모델 순차 시도, 에러 원인 분류) |
| v1.3.1 | Gemini 스마트 폴백 (유료/무료 자동 선택, 쿼터 임시 차단·자동 복귀) |
| v1.3.0 | API 키 인식 오류 수정, 창체·스포츠 태그 대폭 확장, 수업자료 게임 제작 |
| v1.2.0 | 수업 슬라이드 이미지 생성, 수업자료 직접 편집, 퀴즈 앱 개선 |
| v1.1.0 | 슬라이드 이미지 자동 생성, 워크시트 학년별 최적화 |
