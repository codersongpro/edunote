### 설치 방법
1. 아래 Assets에서 `edunote_*.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 바로 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com) 에서 무료로 발급받을 수 있습니다.

---

## v1.6.3 버그 수정

### API 키 저장 후 입력 지연 수정
- API 키 저장 직후 전체 화면 완료 모달을 띄우지 않고, 입력을 막지 않는 토스트 알림으로 변경했습니다.
- 키 테스트는 Flash 모델 중 하나라도 실제 생성에 성공하면 즉시 완료되어 불필요한 대기 시간을 줄였습니다.

### API 사용 가능 상태 문구 정리
- 실제 결과물 생성이 가능한 상태에서만 사이드바에 `API 사용 가능!`을 표시합니다.
- 토큰 소모 또는 잦은 요청으로 당장 생성할 수 없는 경우 `API 사용을 위해 잠시 기다리세요!`로 안내합니다.
- 무료 Gmail 기준 사용성을 위해 기본 생성 모델 우선순위를 Flash 계열로 정리했습니다.

---

## v1.6.2 업데이트

### 릴리즈 메타데이터 정리
- GitHub 최신 릴리즈 `v1.6.1` 기준으로 다음 패치 버전 `v1.6.2`를 발행합니다.
- 패키지 설명과 Windows 빌드 저작권 문구의 깨진 인코딩을 정리했습니다.
- 기존 GitHub Actions Windows 빌드/릴리즈 흐름을 그대로 사용합니다.

### API 키 안내 개선 방향 반영
- 무료 Gmail 계정 사용자는 Gemini 무료 한도가 계정/프로젝트/모델 상태에 따라 달라질 수 있음을 명확히 안내합니다.
- 앱은 정확한 전체 잔여 횟수를 보장하기보다 현재 사용 가능 여부와 오류 원인 중심으로 안내하는 방향을 유지합니다.

---

## v1.6.1 버그 수정

### API 키 테스트 오류 메시지 상세화
- 잘못된 키, 네트워크 오류, 권한 거부, 쿼터 초과, 모델 미지원 등 원인별 안내를 강화했습니다.
- 최후 폴백에서도 실제 오류 메시지와 모델별 결과를 표시합니다.

### 상위 모델 자동 재시도 개선
- 권한 오류로 차단된 모델을 일정 시간 후 다시 시도하여 계정 상태 변화에 대응합니다.

### 설정 화면 빈 화면 버그 수정
- API 키 설정 화면 진입 시 빈 화면이 나오던 문제를 수정했습니다.

---

## v1.6.0 업데이트 요약

- API 키 활성화 팝업 알림 추가
- 사이드바 API 키 상태 즉시 반영
- API 키 검증 속도 개선
- 생성 완료 효과음 안정성 개선
- AI 요청 타임아웃 메시지 개선
