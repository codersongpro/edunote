## EduNote v1.10.5

### 보안 보강
- 설정 조회와 백업 흐름에서 Gemini API 키가 노출되지 않도록 보호했습니다.
- 외부 링크 열기는 `https` 주소만 허용하도록 제한해 비정상 주소 실행 위험을 줄였습니다.
- 스킬마켓에서 외부 도구를 가져올 때 기본 스키마를 검증하고, 위험한 HTML 태그와 속성은 경고 또는 차단하도록 보강했습니다.
- AI 생성 HTML과 외부 HTML을 화면에 표시하는 경로에 sanitizer를 적용해 실행 가능한 스크립트가 동작하지 않도록 했습니다.

### 교사 검토 흐름
- 생성 결과에 검토 체크리스트를 추가해 개인정보, 과장 표현, 금지 표현, 최신 지침 확인, 최종 교사 검토를 점검할 수 있게 했습니다.
- 개인정보 보호 모드를 추가해 AI 요청 전 학생 이름을 임시 토큰으로 바꾸고, 응답 후 원래 이름으로 복원할 수 있게 했습니다.
- 생기부, 교과 세특, 창체, 스포츠 생성 화면에서 학생별 최근 메모를 참고자료로 연결할 수 있게 했습니다.
- 빠른 재작성 버튼을 추가해 더 짧게, 더 공문답게, 더 따뜻하게, 중복 표현 줄이기, NEIS 문체 다듬기를 바로 요청할 수 있게 했습니다.

### 생성 결과 히스토리와 저장
- 생성 결과의 현재 버전을 저장하고, 이전 버전을 선택해 미리 본 뒤 `이 문서로 되돌리기`를 눌렀을 때만 현재 문서에 덮어쓰도록 변경했습니다.
- 현재 버전 저장 시 줄바꿈, 표, 말머리 기호가 유지되도록 HTML 구조를 함께 저장합니다.
- 저장 메뉴를 PDF, Word/HWP용 문서, HTML, Markdown 선택 방식으로 정리했습니다.
- 저장 파일명은 맨 윗줄 전체 대신 제목 또는 주요 내용을 반영하고, 생성 날짜를 포함하는 형식으로 저장됩니다.

### 생성 결과 복사 개선
- 생성 결과 복사 형식에 `한글 최적화 복사`를 추가했습니다.
- 한글 프로그램에 붙여넣을 때 글씨 크기, 표 테두리, 셀 여백, 글자 정렬이 더 잘 유지되도록 복사 직전에 HTML 스타일을 인라인으로 정리합니다.
- 기존 `HWP/웹 복사`, `Excel로 복사`, `MD로 복사`는 그대로 유지했습니다.
- Excel 복사는 표가 셀 단위로 붙여넣어지도록 탭 구분 텍스트를 사용합니다.

### 데모 화면 개선
- 데모 창의 다크모드 마우스오버 상태에서 글씨가 잘 보이지 않던 문제를 수정했습니다.
- 깨져 보이던 데모 한글 문구를 정상 한글 샘플로 정리했습니다.
- 교무행정AI, 학생기록AI, 수업자료AI, AI 스킬즈의 새 메뉴를 시연할 수 있는 데모 샘플을 추가했습니다.

### AI 스킬즈 및 스킬마켓
- 내 스킬 공유 설문에서 `만드신 도구의 종류` 값이 자동으로 채워지도록 보강했습니다.
- 이수증 연수번호 수집기 예시 결과가 성명 기준 가나다 오름차순으로 정렬되도록 안내를 추가했습니다.
- 이수증 연수번호 수집기의 최대 파일 수를 10개로 맞추고, 재실행 시 처리 과정이 다시 정상 표시되도록 정리했습니다.
- 이수증 연수번호 수집기 실행 후 생성 결과 상단의 저장/복사 버튼이 계속 동작하도록 수정했습니다.

### 검토 보조
- `주의어 감지`가 무엇을 뜻하는지 마우스오버 설명을 추가했습니다.

### 설치 방법
1. Assets에서 `edunote_1.10.5_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 앱 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.10.4

### 보안 및 안정성
- 일반 생성 결과의 HWPX 직접 저장 메뉴를 제거했습니다. 기존 방식은 한글 프로그램에서 파일이 깨져 보일 수 있어, 업로드한 HWPX 양식에 데이터를 채워 저장하는 경우에만 HWPX 저장 버튼이 표시됩니다.
- 생성 결과가 갱신될 때 편집 영역이 강제로 포커스를 가져가던 동작을 제거해, 입력 필드가 가끔 잠긴 것처럼 보이는 문제를 줄였습니다.

### 생성 결과 사용성
- 저장 버튼을 하나로 묶고 PDF, Word/HWP용 문서, HTML, Markdown 중 원하는 형식을 선택해 저장할 수 있게 했습니다.
- 복사 버튼을 하나로 묶고 HWP/웹 복사, Excel로 복사, MD로 복사 중 원하는 형식을 선택할 수 있게 했습니다.
- 현재 버전 저장 시 HTML 구조를 함께 보관해 줄바꿈, 표, 말머리 기호가 되돌리기 과정에서 더 잘 유지되도록 했습니다.
- 이전 생성 문서를 선택해 현재 결과로 되돌릴 수 있는 히스토리 흐름을 정리했습니다.

### 도구 수정
- 이수증 연수번호 수집기의 최대 파일 수를 10개로 맞췄습니다.
- 이수증 연수번호 수집기를 다시 실행할 때 이전 결과가 처리 과정을 가리지 않도록 시작 시 결과와 진행 상태를 초기화했습니다.
- 이수증 연수번호 수집기 실행 후 상단 저장/복사 동작이 끊기지 않도록 결과 화면 버튼 구성을 정리했습니다.

### 설치 방법
1. Assets에서 `edunote_1.10.4_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 앱 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.10.1

### 이번 릴리즈 변경 사항

#### 신규
- **HTML 앱 만들기**: AI스킬즈에서 HTML 앱을 직접 만들 수 있습니다. 원하는 앱을 설명하면 AI가 즉시 만들어 줍니다. (팀별 점수판, 수업 타이머, 랜덤 모둠 편성기 등)
- **수업 타이머 샘플 앱 내장**: 활동 이름 입력, 분/초 설정, 카운트다운, 벨 소리, 전체화면 기능이 포함된 수업 타이머가 '내 스킬'에 기본 제공됩니다.

#### 개선
- **메뉴 이름 개선**: 사이드바 'AI스킬즈' → '내 스킬', '내 도구' 탭 → '내 스킬' 탭으로 통일했습니다.
- **스킬 타입 뱃지 표시**: 스킬 카드에 `<앱>` (HTML 앱) / `<AI>` (프롬프트 스킬) 뱃지가 표시됩니다.
- **학교급 자동 적용**: 스킬 실행 시 설정된 학교급(초등·중등·고등)이 AI 결과에 자동 반영됩니다.
- **HTML 앱 내보내기**: HTML 앱 스킬 공유 시 `.html` 파일로 올바르게 저장됩니다. (기존 JSON 저장 오류 수정)
- **앱 만들기 도움말**: HTML 앱 만들기 화면에 구체적인 작성 방법과 예시 칩을 개선했습니다.
- **도구 공유 폼 자동 입력**: 공유 폼 열기 시 도구 이름·설명·카테고리·소속·한 마디가 자동으로 채워집니다.
- **기본 테마를 다크모드로 변경**: 첫 실행 시 다크모드가 기본으로 적용됩니다. (설정에서 변경 가능)

#### 버그 수정
- HTML 앱에서 소리 효과 오류 수정: 깨진 base64 오디오 대신 Web Audio API로 생성합니다.

### 설치 방법
1. 아래 Assets에서 `edunote_1.10.1_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.10.0

### 이번 릴리즈 변경 사항

#### AI스킬즈 메뉴 신규 추가 (사이드바 4번째 상위 메뉴)
- **도구 만들기**: 자주 쓰는 AI 작업 패턴을 코드 없이 직접 만드는 기능
  - 3단계 위저드로 도구 생성 (기본 정보 → 입력 필드 구성 → 프롬프트 작성)
  - 대화형 도구 만들기 (AI와 대화하면 자동으로 도구 초안 생성, 최대 4단계 질문)
  - 입력 필드 3종 지원: 한 줄 텍스트, 여러 줄 텍스트, **파일 첨부** (이미지·PDF·HWPX·Ctrl+V 붙여넣기)
  - 도구 실행 화면에서 AI 결과 생성 및 편집·저장·PDF 내보내기
  - 도구 실행 화면에서 **취소 버튼** 추가 (생성 중단 가능, 90초 타임아웃 자동 적용)
  - 파일 업로드 **최대 20개** 동시 처리 지원 (진행 상황 표시)
  - 위저드 3단계 및 대화형 만들기에서 Ctrl+V 스크린샷 붙여넣기 지원 개선
- **마켓플레이스**: 동료 선생님 또는 커뮤니티에서 도구를 받아오는 기능
  - 구글 폼으로 JSON 파일 업로드 → 공유 목록에서 원클릭 가져오기
  - JSON 파일 직접 내보내기·가져오기 (카카오톡·이메일 공유)
  - 마켓 도구 카드에 **소속 학교·한 마디** 표시 (공유자 정보)
  - **내 도구 카드에서 바로 공유하기** 버튼 추가 (이름·소속·한 마디 입력 → 2단계 공유 안내)

#### 샘플 도구 개선
- **이수증 연수번호 수집기**: 성명 컬럼 추가, 여러 파일을 표 하나로 통합, 엑셀·구글 시트 붙여넣기 가능한 형식으로 출력

#### 버그 수정
- 도구 실행 중 AI 생성이 멈추는 문제 수정 (90초 타임아웃 적용)
- 마켓에서 도구 가져오기 클릭 시 앱이 오류 창을 띄우는 문제 수정 (Google Drive 응답 헤더 인코딩 처리)
- 도구 만들기 위저드에서 Ctrl+V 스크린샷 붙여넣기가 작동하지 않던 문제 수정

### 설치 방법
1. 아래 Assets에서 `edunote_1.10.0_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.9.2

### 이번 릴리즈 변경 사항
- Demo 버튼 클릭 시 별도 새 창으로 열립니다. (이미 열려 있으면 해당 창으로 포커스 이동)
- 메인 화면에서 카드 텍스트가 틀을 벗어나던 오버플로우 문제를 수정했습니다.
- 메인 화면 바로가기 카드 순서를 변경했습니다: 설정(왼쪽) · 사용방법(오른쪽).
- 메인 화면 제작자 정보 블록 및 '개발자의 다른 앱 보기' 버튼을 제거했습니다.
- 수업계획서·학습지 생성 시 표(테이블)가 누락되던 문제를 수정했습니다.
- 메인 화면 안내 문구가 두 줄로 줄바꿈되던 문제를 수정했습니다.
- 파일 첨부 시 Markdown(.md) 파일을 지원합니다.
- 데모 샘플 내용을 '학생 AI 탐구 동아리' 주제로 전면 업데이트했습니다.
- 워크시트 제목을 가운데 정렬·크기 확대하고, 학년/반/이름 기입란을 오른쪽 정렬 및 작성 공간(밑줄) 형태로 개선했습니다.
- 워크시트에 AI 이미지 삽입 기능을 추가했습니다. 이미지 설명을 직접 입력하거나 비워두면 수업 주제 기반으로 자동 생성합니다.
- 데모 공고문 샘플을 외부 강사 모집 공고로 수정했습니다.
- 공문 업무 추출 → 구글캘린더 추가 시, '마감/까지/기한' 키워드 기준으로 마감 날짜를 우선 반영합니다.
- 워크시트 이미지 생성 안정성을 높였습니다. (Imagen 4 폴백 추가, 실패 시 안내 메시지 표시)
- 워크시트 제목 가운데 정렬 및 학년/반/이름 기입란 오른쪽 정렬이 항상 적용되도록 수정했습니다.
- 모든 생성물의 날짜·학년도 기준을 사용자의 현재 날짜로 자동 적용합니다. (공문서 학년도 기본값, 수업자료·학생기록 등 날짜 참조 포함)

### 설치 방법
1. 아래 Assets에서 `edunote_1.9.2_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.9.1

### 이번 릴리즈 변경 사항
- 학생기록AI 메뉴 구조 개선: 행발생성·교과세특·스포츠클럽·창체를 '생기부도우미'로 묶어 트리로 펼쳐집니다.
- '우리반기록' 트리 서브메뉴: 수업관찰기록·상담일지·학급경영일지·학생 메모 보드를 클릭해 바로 이동할 수 있습니다.
- 수업자료AI > 수업 도구: QR 메이커·럭키드로우 서브메뉴로 펼쳐집니다. ('오늘의 주인공' → '럭키드로우' 이름 변경)
- 사이드바 메뉴가 펼쳐질 때 스크롤로 전체 항목을 볼 수 있습니다.
- Demo 버튼을 사이드바 하단으로 이동했습니다.
- 사이드바 설정 버튼 클릭 시 화면이 표시되지 않던 버그를 수정했습니다.
- '공문 요약 / 업무 추출' 메뉴 레이블을 '공문요약·업무추출'로 단축했습니다.
- 품의서 예시 문서: 제목-본문 사이 표를 제거하고 끝 문구(붙임 지출품의서 1부. 끝.)를 추가했습니다.

### 설치 방법
1. 아래 Assets에서 `edunote_1.9.1_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.9.0

### 이번 릴리즈 변경 사항
- 수업관찰기록·상담일지·학급경영일지를 '교사 기록' 하나의 메뉴로 통합했습니다 (탭으로 전환).
- QR 메이커·오늘의 주인공을 '수업 도구' 하나의 메뉴로 통합했습니다 (탭으로 전환).
- 사이드바에 '설정' 버튼을 추가했습니다 (홈 바로 아래).
- 사이드바에 'Demo' 버튼을 추가했습니다 — 시연용 샘플 입력값을 복사 버튼으로 빠르게 활용할 수 있습니다.
- '문자&소통메세지' 메뉴를 '메세지'로 변경하고, 탭 내부에서 '문자(SMS/LMS)'와 '소통메세지'를 선택할 수 있게 개선했습니다.
- 공문 복잡도 툴팁이 입력 패널에 가려지던 문제를 수정했습니다 (아래 방향으로 표시).
- 공문서 작성 입력 패널 너비를 넓혔습니다.
- 예시 문서(공문서·계획서·보고서·공고문)의 좌우 여백을 개선했습니다.

### 설치 방법
1. 아래 Assets에서 `edunote_1.9.0_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.8.8

### 이번 릴리즈 변경 사항
- 공문서 작성에서 '공문 유형(내부결재/대외공문)' 선택 항목을 제거했습니다.
- 공문 복잡도(간단/중간/상세) 버튼에 마우스 오버 시 설명 툴팁을 추가했습니다.
- 협의록 생성 시 학교 이름이 중복 출력되던 버그를 수정했습니다.

### 설치 방법
1. 아래 Assets에서 `edunote_1.8.8_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.

---

## EduNote v1.8.7

### 이번 릴리즈 변경 사항
- 메인 화면 글씨 크기를 키우고 어색한 줄바꿈을 개선했습니다.
- '개발자의 다른 앱 보기' 버튼을 메인 화면 최하단으로 이동했습니다.

### 설치 방법
1. 아래 Assets에서 `edunote_1.8.7_portable.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 설정에서 Gemini API 키를 입력하면 사용할 수 있습니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com)에서 무료로 발급받을 수 있습니다.
# EduNote v1.10.3

## 보안 보강
- 무료/유료 Gemini API 키가 설정 조회 응답에 포함되지 않도록 수정했습니다.
- 외부 링크 열기는 올바른 HTTPS URL만 허용하도록 강화했습니다.
- 외부 도구 가져오기 시 JSON 구조와 HTML 앱 위험 요소를 검사합니다.
- 생성 결과 미리보기 HTML에 기본 살균 처리를 적용했습니다.

## 검토 흐름 개선
- 생성 결과 화면에 검토 체크리스트를 추가했습니다.
- 생성 결과를 빠르게 다시 다듬는 버튼을 추가했습니다.
- 학생 기록 생성 시 학생 메모 참고자료를 함께 반영할 수 있도록 연결했습니다.
- 개인정보 보호 모드를 추가해 학생 이름을 임시 토큰으로 치환한 뒤 결과에서 복원합니다.
