## EduNote v1.15.5

### QR 채팅방 도배 방지
- 학생이 메시지를 너무 빠르게 연속으로 보내지 못하도록 **전송 속도 제한**을 추가했습니다. 최소 전송 간격을 두고, 짧은 시간(6초)에 5번을 넘게 보내면 잠시(약 6초) 전송이 막히며 "도배 방지" 안내가 표시됩니다. 일반적인 대화 속도에는 영향이 없고, 같은 말을 도배하거나 전송을 연타하는 것을 막아줍니다.

### 안내 영상 버튼 추가 및 줄바꿈 정리
- **안내 영상 버튼 추가**: 채팅방 설정 화면에 `채팅방 설정 도움받기 (영상)` 버튼을, 사용 방법 화면 상단에 `에듀노트 전체 사용법 보기 (영상)` 버튼을 추가했습니다. 버튼을 누르면 기본 브라우저에서 안내 영상이 바로 열립니다.
- **빈 화면 안내 문구 줄바꿈 정리**: 자료 생성 화면 등에서 안내 문구가 화면 폭에 따라 단어 중간에서 어색하게 끊기던 문제를 고쳤습니다. 문장을 강제로 끊는 줄바꿈 대신 어절 단위로 자연스럽게 줄바꿈되도록 정리했습니다.

### 나만의 자료실 — 분류(주제) 바로 추가 수정
- 자료 카드의 분류 태그를 눌렀을 때 나오는 드롭다운에 **새 주제 입력란**을 항상 표시하도록 고쳤습니다. 이미 등록된 자료라도 드롭다운에서 새 주제를 입력해 바로 만들고 그 자리에서 적용할 수 있습니다. (이전에는 주제가 하나라도 등록돼 있으면 새 주제를 만들 방법이 없어, 기존 주제 중에서만 골라야 했습니다.)

### 모바일 채팅 화면 가독성·입력창 개선
- **입력창이 화면 밖으로 밀리던 문제 수정**: 태블릿·스마트폰으로 채팅방에 참여할 때 입력창이 아래로 밀려 스크롤해야 보이던 문제를 고쳤습니다. (입력창 글씨를 키워 아이폰·아이패드에서 입력창을 누를 때 화면이 자동 확대되던 현상을 막고, 화면 전체 스크롤을 잠가 입력창이 항상 하단에 고정되도록 했습니다.)
- **글씨 크기 확대**: 채팅 입력창·전송 버튼·말풍선·방 제목 등 모바일 채팅 화면의 글씨를 전반적으로 크게 키워 가독성을 높였습니다.
- **입력창 하단 고정 강화**: 일부 태블릿·스마트폰에서 키보드가 올라오거나 화면 높이 계산이 어긋날 때 입력창이 밀려 스크롤해야 보이던 문제를, 실제로 보이는 화면 영역에 맞춰 입력창을 항상 화면 맨 아래(키보드 위)에 고정하도록 보강했습니다.

### 채팅방 재진입 시 입력 잠김(20~30초) 수정
- 채팅방 화면을 다시 열 때, 느린 네트워크(학교망 등)에서 접속 준비(익명 인증)와 자료 복원이 끝날 때까지 화면 전체가 `불러오는 중...` 상태로 멈춰 **채팅방 제목조차 입력할 수 없던** 문제를 고쳤습니다. 이제 화면이 즉시 열려 바로 제목을 입력하고 새 채팅방을 만들 수 있으며, 접속 준비·복원은 화면을 막지 않고 뒤에서 진행됩니다. (그동안 "채팅방 시작 버튼이 먹통", "종료 후 새 채팅방 개설 불가"로 보이던 현상의 실제 원인이었습니다.)

### 종료된 채팅방에서 학생끼리 대화되던 문제 수정
- 교사가 채팅방을 종료(또는 삭제)한 뒤에도 학생들이 그 방에 남아 서로 계속 대화할 수 있던 문제를 고쳤습니다. 보안 규칙에 **"종료된 방에는 교사·학생 누구도 메시지를 보낼 수 없음"**을 추가해 서버에서 직접 막고, 학생 화면도 방이 삭제돼 사라진 경우까지 "종료" 상태로 잠그도록 했습니다.
- **중요**: 이 수정을 적용하려면 채팅방 설정 화면의 `규칙 복사` 버튼으로 최신 보안 규칙을 복사해 Firebase 콘솔의 Firestore Database → 규칙 탭에 붙여넣고 **다시 게시**해야 합니다. (보안 규칙 내용이 이번에 바뀌었습니다.)

### 교사 채팅 화면 입력창 하단 고정
- 교사 채팅 화면에서도 메시지 입력창이 항상 화면 맨 아래에 고정되도록 했습니다. 이제 메시지 목록만 안에서 스크롤되고, 입력창과 상단 버튼(채팅방 종료·새 채팅방 만들기)은 스크롤과 상관없이 늘 보입니다. (지난 채팅방 목록은 채팅방을 시작하기 전 화면에서 확인할 수 있습니다.)

### 채팅방을 QR 화면과 대화 창으로 분리
- 채팅방의 **QR코드·접속주소는 에듀노트 본 창**(`수업 도구 → 채팅방` 탭)에 그대로 표시되고, **대화(참여자·메시지·입력창)는 별도 새 창**으로 분리해 열리도록 바꿨습니다. 채팅방을 시작하면 대화 창이 자동으로 뜨며, 본 창에서는 QR코드·접속주소만 크게 보여줄 수 있어 **QR코드·접속주소 뒤로 대화가 가려져 보이지 않던 문제**가 사라졌습니다. (QR을 화면에 띄워 학생을 입장시키면서, 교사는 대화 창에서 메시지를 주고받습니다.)
- **접속 주소 복사 버튼**을 추가했습니다. QR 촬영이 어려운 학생에게는 접속 주소를 복사해 바로 전달할 수 있습니다.
- 대화 창은 닫아도 채팅방이 종료되지 않습니다(QR이 본 창에 그대로 유지됩니다). 채팅방 종료는 본 창의 `채팅방 종료` 버튼으로만 이루어지며, 닫은 대화 창은 `대화창 열기` 버튼으로 다시 열 수 있습니다.
- 창 크기에 맞춰 QR코드 크기도 자동으로 조절됩니다.

### 채팅방 접속 주소 단축 및 QR 스캔 개선
- **접속 주소를 약 절반으로 줄였습니다.** 접속 주소에 담기던 연동 정보 중 학생 입장에 꼭 필요한 값만 남기고 나머지는 빼서(빠진 값은 학생 화면이 자동으로 채웁니다), 주소가 짧아지고 QR코드도 덜 빽빽해졌습니다.
- **QR코드가 더 잘 스캔되도록** QR의 정밀도(오류정정) 수준을 화면 표시에 알맞게 조정해, 같은 크기에서도 칸이 커져 휴대폰으로 더 쉽게 인식됩니다.
- 이전에 만들어 둔(긴 형식) 접속 주소·QR코드도 그대로 사용할 수 있습니다(하위 호환).

### 안정성 보완
- 예기치 못한 내부 오류가 조용히 묻히지 않고 기록으로 남도록 보완해, 드물게 발생하는 문제의 원인 파악을 쉽게 했습니다.
- 데이터·백업 파일이 손상된 경우 앱이 오류로 멈추지 않고 "백업 파일이 손상되어 읽을 수 없습니다" 같은 안내를 보여주도록 처리했습니다.

### 채팅방 다크 모드 대응
- 채팅방 화면(본 창의 채팅방 탭과 대화 창)이 **다크 모드에서도 어두운 테마로 표시**되도록 했습니다. 그동안 다른 화면은 어두운데 채팅방만 흰 화면으로 보이던 문제가 사라졌습니다.

### 백업 권장 안내
- 홈 화면에서 **아직 백업한 적이 없거나 마지막 백업이 2주 이상 지난 경우** 부드러운 백업 권장 안내와 `지금 백업` 버튼을 표시합니다. PC 교체·재설치 시 자료를 잃지 않도록 백업을 잊지 않게 도와줍니다.

### 소소한 사용성 보완
- 나만의 자료실을 열 때 자료를 불러오는 동안 "불러오는 중입니다" 표시를 추가해, 빈 목록이 잠깐 보이며 "자료가 사라졌나?" 오해하던 점을 줄였습니다.

---

## EduNote v1.15.4

### QR 채팅방 종료·학생 퇴장 개선
- **채팅방 강제 종료 + 학생 퇴장**: 교사가 "채팅방 종료"를 누르면(확인 후) 모든 학생 화면이 "채팅방이 종료되었습니다. 더 이상 참여할 수 없습니다." 전체 화면으로 잠겨 사실상 퇴장 처리됩니다. (기존에는 입력만 막혔습니다.)
- **종료가 안 되던 권한 오류 수정**: 익명 로그인 정보를 같은 PC에서 계속 유지하도록 고쳐(앱 시작 시 저장된 계정 복원을 기다린 뒤 사용), 내가 만든 채팅방의 소유권이 유지되어 항상 종료·삭제할 수 있습니다. (이전에는 앱 재시작·재설정 후 계정이 바뀌어 "Missing or insufficient permissions" 오류로 종료가 막힐 수 있었습니다.) 이미 다른 계정으로 만들어진 옛 방을 종료하려 하면, 그 사실을 알려주는 안내 문구를 표시합니다.

> 참고: 학생 화면에 "귓속말을 불러오지 못했습니다(permission-denied)"가 보이면, 채팅방 설정 화면에서 **보안 규칙을 다시 복사해 Firebase 콘솔에 게시**한 뒤 학생 화면을 새로고침해 주세요. (현재 앱의 규칙이 최신이며, 콘솔에 옛 규칙이 남아 있으면 귓속말만 막힙니다.)

---

## EduNote v1.15.3

### 수업 도구 튜토리얼 개선
- 수업 도구(QR 메이커·럭키드로우·채팅방) 튜토리얼에서 일부 단계가 화면에 보이지 않는 탭 내용을 가리켜 하이라이트가 어긋나던 문제를 고쳤습니다. 이제 각 단계가 항상 보이는 **탭 버튼**을 정확히 강조합니다.
- 채팅방 단계 설명을 보강했습니다. 처음 한 번 필요한 무료 Firebase 연동(익명 로그인 켜기, 데이터베이스 위치 "서울" 선택)과, 귓속말(여러 명 선택)·공지 고정·참여자 확인·대화 저장 등 채팅방의 주요 기능을 안내합니다.

---

## EduNote v1.15.2

### QR 채팅방 실시간 전달·귓속말 수정
- **메시지가 실시간으로 바로 전달되도록 수정**: 메시지를 보내도 상대방 화면에 바로 보이지 않고, 다음 메시지를 보내야 그제서야 전달되던 문제를 고쳤습니다. 학교망 등 일부 네트워크가 실시간 스트리밍 연결을 버퍼링해 생기던 현상으로, 연결 방식을 변경 사항마다 즉시 주고받는 방식(롱폴링)으로 바꿔 해결했습니다. (교사 앱과 학생 화면 모두 적용)
- **귓속말이 전달되지 않던 문제 수정**: 귓속말 읽기 보안 규칙에 들어 있던 형식 검사(`is list`) 때문에 학생 쪽 귓속말 조회가 통째로 거부되던 문제를 고쳤습니다. 보안 규칙에서 해당 검사를 빼 표준 방식으로 바꿨습니다.
- 학생 화면에서 메시지가 안 보일 때, 공개 메시지와 귓속말의 오류를 따로 표시하도록 개선했습니다(한쪽이 정상이어도 다른 쪽 오류 안내가 가려지지 않습니다).

> **중요**: 위 귓속말 수정을 적용하려면 채팅방 설정 화면에서 **보안 규칙을 다시 복사해 Firebase 콘솔에 게시**해야 합니다. (이번에 보안 규칙 내용이 바뀌었습니다.) 게시 후 학생 화면은 새로고침해 주세요.

---

## EduNote v1.15.1

### QR 채팅방 사용성 개선
- **보낸 메시지가 늦게 보이던 문제 수정**: 학생 화면에서 방금 보낸 메시지가 바로 보이지 않고, 다음 메시지를 보내야 직전 메시지가 나타나던 현상을 고쳤습니다. (서버 시각이 확정되기 전의 메시지를 목록 맨 아래에 바로 표시하도록 정렬을 다듬었습니다.)
- **입력창 고정**: 새 메시지가 올라올 때 입력창이 위로 밀려 올라가던 현상을 고쳐, 교사·학생 모두 메시지 영역만 스크롤되고 입력창은 화면 아래에 고정되도록 했습니다.
- **설정 가이드에 지역 안내 추가**: Firestore 데이터베이스를 만들 때 위치를 **서울(asia-northeast3)** 로 선택하도록 안내를 추가했습니다. 서버가 가까울수록 메시지가 빠르게 오가며, 지역은 만든 뒤 바꿀 수 없다는 점도 함께 안내합니다.

> 참고: 학생 화면에 메시지가 보이지 않거나 귓속말이 동작하지 않으면, 채팅방 설정 화면에서 **보안 규칙을 복사해 Firebase 콘솔에 다시 게시**한 뒤 학생 화면을 새로고침해 주세요. (교사 화면은 방을 만든 사람이라 규칙과 무관하게 동작하지만, 학생 화면은 최신 보안 규칙이 게시되어 있어야 메시지를 받을 수 있습니다.)

---

## EduNote v1.15.0

수업자료AI > 수업 도구에 **QR 채팅방**을 새로 추가했습니다. 학생은 로그인이나 앱 설치 없이 QR코드만 스캔해 닉네임을 입력하면 바로 참여할 수 있습니다.

### 채팅방 시작과 연동
- 채팅방은 각 교사가 직접 만드는 **무료 Firebase 프로젝트**로 동작합니다. 처음 한 번 앱 안내를 따라 설정값을 붙여넣으면 연동이 끝나며, 학교 와이파이(학생망)와 교사 PC가 같은 네트워크가 아니어도 정상 동작합니다.
- 설정 안내에 **익명 로그인 사용 설정** 단계를 포함했습니다. 연동이 끝나면 "설정 완료" 안내와 **다시 설정하기** 버튼이 표시되며, 설정 중 오류가 나면 무슨 조치가 필요한지 한국어로 안내합니다.
- 채팅방을 시작할 때 **제목**(예: "3교시 모둠활동")을 정할 수 있습니다. 제목은 교사 화면·지난 채팅방 목록·학생 접속 화면에 모두 표시됩니다.
- 보안을 위해 **방 코드를 정확히 아는 사람만** 접근할 수 있고(전체 방 목록 조회 차단), 메시지·닉네임 길이 등 형식을 검증합니다.
- ⚠️ 채팅방을 사용하려면 채팅방 설정 화면에서 **보안 규칙을 복사해 Firebase 콘솔의 규칙 탭에 붙여넣고 게시**해야 합니다. 규칙을 게시하지 않으면 학생 화면에 메시지가 보이지 않을 수 있습니다.

### 채팅 기능
- 텍스트와 링크를 주고받을 수 있고(웹 주소는 자동으로 클릭 가능한 링크가 됩니다), 교사도 같은 화면에서 학생들과 함께 채팅합니다.
- 카카오톡처럼 **내 메시지는 오른쪽, 상대 메시지는 왼쪽**에 표시되고 보낸 사람마다 말풍선 색이 다릅니다.
- **참여자 목록**으로 누가 들어와 있는지 확인할 수 있습니다(초록 점=접속 중, 회색 점=접속 끊김).
- **귓속말**: 참여자 이름을 누르면 그 학생에게만 보이는 메시지를 보낼 수 있습니다. 이름을 여러 명 선택하면 **한 번에 여러 명에게** 보낼 수 있고, 귓속말은 보라색 말풍선으로 구분됩니다. (귓속말 비공개는 익명 인증 기반 보안 규칙으로 실제로 보장되며, 학생이 따로 할 일은 없습니다.)
- **공지 고정**: 메시지 옆 📌 아이콘을 누르면 그 메시지가 채팅창 맨 위에 고정되어 모두에게 항상 보입니다(✕로 해제, 귓속말은 고정 불가).
- 대화 내용을 **텍스트 파일로 다운로드**할 수 있습니다(진행 중인 방·지난 방 모두).
- 같은 기기(같은 브라우저)에서의 **중복 접속을 차단**합니다. (다른 브라우저·다른 기기까지는 감지하지 못합니다.)

### 채팅 기록 관리
- 채팅 기록은 Firebase에 보존되어 EduNote를 다시 실행해도 지난 채팅방과 메시지를 확인할 수 있습니다. (지난 채팅방 목록은 사용 중인 PC에 저장됩니다.)
- 채팅방을 종료하거나 EduNote 창을 닫으면 학생은 더 이상 메시지를 보낼 수 없으며, 종료 후 **새 채팅방 만들기**로 곧바로 다음 방을 시작할 수 있습니다.
- 지난 채팅방 목록의 **삭제** 버튼은 Firebase에 저장된 대화 내용·참여자 목록·방 자체를 **영구 삭제**합니다(되돌릴 수 없음).

---

## EduNote v1.14.2

### Gemini API 키 안내 개선
- 처음 실행할 때 나오는 온보딩 화면에서 Gemini API 키를 붙여넣은 뒤 바로 테스트하고 저장할 수 있도록 개선했습니다.
- API 키를 저장하거나 삭제하면 홈 화면의 API 키 상태가 바로 갱신되도록 수정했습니다.
- API 키가 저장되어 있는데도 홈 화면에 `API 키 필요`로 표시되던 문제를 고쳤습니다.
- Google AI Studio에서 `edunote` 프로젝트를 만들어 API 키를 발급받는 방법과, Google Cloud를 활용한 대안 안내를 앱 안의 안내 문구에 반영했습니다.

### 온보딩 2단계 UI 수정
- 온보딩 2단계 모달이 화면 높이를 넘을 때 다음 단계 버튼이 잘려 보이지 않던 문제를 수정했습니다.
- API 키 발급 안내에서 "첫 번째 방법 / 두 번째 방법" 표현을 제거했습니다. 기본 안내(AI Studio)와 Google Cloud 대안 방법으로 명확히 구분합니다.
- Google Cloud 대안 방법은 기본적으로 접혀 있으며, 필요할 때 펼쳐 볼 수 있도록 변경했습니다.
- 발급 단계 번호 원이 긴 안내 텍스트와 어긋나던 정렬을 수정했습니다.

### API 키 발급 도움 영상 안내 추가
- 온보딩 2단계, 설정 화면, 사용 방법 화면의 API 키 발급 안내에 `키 발급 도움받기 (영상)` 버튼을 추가했습니다. 누르면 발급 과정을 보여주는 안내 영상을 바로 열 수 있습니다.

### 스킬마켓·내 스킬 동기화 버그 수정
- 스킬마켓에서 스킬을 가져왔는데 내 스킬 목록에 보이지 않던 문제를 고쳤습니다.
- 내 스킬에서 스킬을 삭제해도 스킬마켓에서는 계속 "추가됨"으로 표시되던 문제를 고쳤습니다.
- 두 화면이 내부적으로 서로 다른 인스턴스로 분리되어 상태가 따로 관리되던 것이 원인이었으며, 하나의 화면을 함께 쓰도록 수정했습니다.
- 가져온 스킬의 작성자·생성일 정보를 마켓 등록 정보 기준으로 보정하고, 가져온 스킬의 toolType 값을 내부 표준값으로 정리했습니다.

---

## EduNote v1.14.1

### 번역 후 표가 화면 밖으로 넘치던 문제 수정
- 가정통신문처럼 표가 있는 문서를 번역하면, 한글보다 긴 외국어 단어 때문에 표가 미리보기 화면 밖으로 넘치던 문제를 고쳤습니다.
- 표 칸 너비를 고정하고 긴 단어는 칸 안에서 줄바꿈되도록 해, 번역 후에도 표가 A4 폭 안에 들어옵니다. PDF·Word·HWPX로 저장한 파일에도 동일하게 적용됩니다.

### 데모(시연 샘플) 보강
- 각 시연 샘플에 **무엇을 하는 기능인지** 설명, **사용 순서**, **기대 결과**를 추가해, 기능을 처음 보는 분도 쉽게 따라 할 수 있게 했습니다.
- 데모의 문서작성기 샘플 이름을 실제 탭 이름(회의록·홍보자료)과 일치시켰습니다.

### 메뉴별 인터랙티브 튜토리얼(시작)
- 화면 위에서 실제 버튼·입력칸을 단계별로 짚어 주는 **따라 하기** 튜토리얼을 추가했습니다.
- 문서작성기·수업자료 생성·예산안작성 화면의 `튜토리얼` 버튼으로 시작할 수 있습니다. 앞으로 다른 메뉴로도 확대할 수 있는 구조로 만들었습니다.

---

## EduNote v1.14.0

### HWPX 저장 — 글자 겹침 해결과 기본 포맷 적용
- 긴 문단이 한글에서 겹쳐 보이던 문제를 고쳤습니다. 문단의 줄 수를 추정해 줄마다 줄 배치 정보(lineseg)를 생성하므로, 여러 줄로 넘어가는 문단도 정상적으로 표시됩니다.
- HWPX 저장 기본 포맷을 정했습니다. 미리보기와 저장 파일의 모양 차이가 크게 줄어듭니다.
  - **제목**: 22pt 굵게·가운데 정렬
  - **기관명**(오른쪽 정렬 줄)·**본문**·**표 안 내용**: 14pt
  - **"1." 수준** 문단: 15pt
  - **"가." 수준**: 두 칸 들여쓰기, **"1)" 이하 수준**: 단계마다 두 칸씩 더 들여쓰기
- 본문에 없는 "생성문서" 같은 가짜 제목이 문서 맨 위에 추가되던 문제를 고쳤습니다. 이제 HWPX에는 미리보기에 보이는 내용만 들어갑니다.
- HWPX 파일명에도 다른 형식과 동일하게 날짜가 붙습니다: `(YYMMDD)_제목.hwpx`
- 문서작성기·수업자료 등 HWPX 저장을 쓰는 모든 화면에 동일하게 적용됩니다.
- 양식 인쇄 도구에서도 화면에서 직접 고친 양식을 HWPX로 저장할 수 있습니다.
- 양식 인쇄 도구에서 HWPX 저장 시 CSS 스타일 코드가 본문에 들어가던 문제를 고쳤습니다.

### 가정통신문·메시지 다국어 번역
- 가정통신문·메세지 생성 결과 화면에 **번역** 버튼이 생겼습니다. 언어를 선택하고 번역을 누르면 번역문이 원문 아래에 함께 들어가, 그대로 인쇄·저장해 다문화 가정에 배포할 수 있습니다.
- 지원 언어(13개): 영어, 중국어(간체), 일본어, 베트남어, 필리핀어(타갈로그), 러시아어, 우즈베크어, 몽골어, 태국어, 캄보디아어(크메르), 인도네시아어, 네팔어, 미얀마어
- 학교명·이름·날짜·연락처는 번역하지 않고 그대로 둡니다. `원문만` 버튼으로 번역문을 제거할 수 있습니다.

### 간단 번역 도구
- 교무행정AI에 `간단 번역` 메뉴가 생겼습니다. 학부모 안내 문구, 문자, 알림장 내용을 13개 언어로 빠르게 번역하고, 외국어를 한국어로 번역할 수도 있습니다.

### 공문 할일 목록
- 교무행정AI에 `공문 할일` 메뉴가 생겼습니다. 공문요약·업무추출 결과 화면의 **할일로 저장** 버튼을 누르면 업무 제목과 마감일이 자동으로 추출되어 할일 목록에 쌓입니다.
- 할일은 마감일 순으로 정렬되고 D-day 배지가 표시됩니다. 직접 추가·완료 체크·삭제할 수 있습니다.
- 공문요약 화면에서 할일로 저장한 뒤 공문 할일 화면이 이미 열려 있어도 새 항목이 바로 반영됩니다.
- 앱을 시작할 때 마감이 3일 이내이거나 지난 할일이 있으면 알려 줍니다.
- 할일 데이터는 앱 데이터 폴더에 저장되어 전체 자료 백업에 자동 포함됩니다.

### 자동 정기 백업
- 설정 → 전체 자료 백업에 **자동 정기 백업** 주기를 추가했습니다(매주 기본 / 매일 / 사용 안 함).
- 앱을 시작할 때 주기가 지났으면 앱 데이터 폴더의 `backups`에 자동으로 백업 파일을 만듭니다. 최근 10개만 보관하며, API 키는 포함하지 않습니다.

### 데모와 메뉴 정리
- 앱 안의 Demo 샘플에 자동 정기 백업, 공문 할일 저장, 다국어 번역, HWPX 저장 확인, 간단 번역 예시를 추가했습니다.
- 홈과 설정 버튼에 섹션 성격에 맞는 컬러를 적용했습니다.
- 사이드바 `AI 스킬즈` 아래에 `내 스킬`, `스킬마켓` 하위메뉴를 추가하고, 다른 하위메뉴처럼 드래그로 순서를 바꿀 수 있게 했습니다.

### 양식 인쇄 도구 개선
- 학교 양식의 별도 입력 패널을 없애고, A4 양식 화면을 직접 클릭해 수정하는 방식으로 바꿨습니다.
- 직접 수정한 현재 화면 내용을 기준으로 PDF 저장·인쇄와 HWPX 저장을 할 수 있습니다.

### 문서작성기 개선
- 보도자료 작성 화면에 제목 입력칸을 추가했습니다.
- 문서작성기에서 파일을 저장할 때 입력한 문서 제목이 파일명에 우선 반영되도록 했습니다.

---

## EduNote v1.13.4

### HWPX 저장 — 한글에서 열리지 않던 문제 해결
- 생성 결과를 `HWPX`로 저장한 파일이 한글(HWP)에서 열리지 않던 문제를 고쳤습니다. 한글이 직접 저장한 정상 .hwpx 파일과 구조를 1:1로 대조해 세 가지 원인을 찾아 모두 수정했습니다.
  - 압축 파일의 첫 항목(mimetype)이 압축되어 있어 한글이 HWPX 파일로 인식하지 못하던 것을, 한글 원본과 동일하게 무압축으로 저장하도록 바꿨습니다.
  - 모든 문단에 한글이 기대하는 줄 배치 정보(linesegarray)를 포함했습니다.
  - 표를 텍스트로 바꿀 때 들어가던 탭 문자가 문서 손상으로 이어지던 것을, 한글 고유의 탭 요소로 변환하도록 했습니다.

### HWPX 저장 — 서식 유지
- HWPX로 저장할 때 문서 서식이 함께 유지됩니다. 이전에는 모든 내용이 같은 크기의 일반 텍스트로 저장되었습니다.
  - **제목**: 큰 글씨(22pt)·굵게·가운데 정렬로 저장됩니다. 절 제목(Ⅰ., Ⅱ. 등)은 14pt 굵게.
  - **문단 정렬**: 가운데·오른쪽 정렬(예: 소속기관명)이 유지됩니다.
  - **표**: 진짜 한글 표로 저장됩니다. 테두리·머리글 행(굵게·가운데)·셀 병합(colspan/rowspan)까지 반영됩니다.
  - **굵은 글씨**(강조)와 **목록**(•, 1. 2. 3.)도 유지됩니다.
- 문서 구조를 해석할 수 없는 예외적인 경우에는 이전처럼 텍스트로 저장해 어떤 경우에도 파일이 열리도록 했습니다.

---

## EduNote v1.13.3

### HWPX(한글 문서) 저장 복구
- 한글(HWP)에서 "사용할 수 없는 파일"로 표시되어 꺼 두었던 HWPX 저장을 수리해 다시 켰습니다.
- 원인은 파일 재포장 과정에서 원본에 없던 폴더 항목이 추가되는 등 한글이 기대하는 압축 파일 구조가 깨지는 것이었습니다. 이제 실제 한글이 만드는 파일과 동일한 구조(항목 구성·순서·압축 방식)로 저장합니다.
- 내장 HWPX 생성도 필수 구성요소(version.xml, header.xml 등)가 빠진 채 임의 구조로 만들던 것을, 한글이 실제로 여는 검증된 빈 문서를 골격으로 사용해 본문을 채워 넣는 방식으로 바꿨습니다.
- 문서작성기에서 HWPX 양식을 업로드한 경우 `HWPX 양식 저장` 버튼이 다시 표시됩니다. 양식의 표 구조·서식은 그대로 유지되고 텍스트 칸만 채워집니다.
- 생성 결과 화면의 저장 형식 목록에 `HWPX`를 추가했습니다. 양식 업로드 없이도 현재 문서를 바로 .hwpx 파일로 저장할 수 있습니다. (표 등 복잡한 서식은 텍스트로 변환)

### 문서작성기 — 입력칸을 다 채우지 않아도 완성 문서 생성
- 문서작성기(공문·계획서·보고서·품의서·협의록·보도자료·가정통신문·메세지·공고문)에서 일부 입력칸만 채워도, 비워 둔 항목은 제목·주제에 어울리는 내용으로 AI가 자연스럽게 추정해 완성된 문서를 작성합니다.
- 일시·기간이 비어 있으면 학년도와 오늘 날짜를 참고해 자연스러운 일정을 제안하고, 결과물에 "(미입력)" 같은 표시는 남기지 않습니다.
- 근거 법령·공문번호는 추정 대상에서 제외됩니다. 입력하지 않은 근거는 기존처럼 사용자가 채울 자리로 표시되어 잘못된 법령·공문번호가 들어가는 일을 막습니다.

### 생성이 끝나지 않는 문제 수정
- 문서 생성 중 AI가 글자 없는 빈 문단만 끝없이 반복 출력하여 생성이 영원히 끝나지 않던 문제를 고쳤습니다. 이런 비정상 반복을 감지하면 즉시 중단하고 다음 모델로 자동 전환합니다.
- 스트리밍 응답이 중간에 멈춘 채 45초 이상 이어지지 않으면 기다리지 않고 다음 모델로 전환합니다.
- 모든 모델에서 실패한 경우에는 원인(같은 내용 반복/응답 중단)을 알려주는 안내가 표시됩니다.

### AI스킬즈 — 대화로 만들기 고도화
- 대화로 만들기에서 만들 결과물 형태를 먼저 선택할 수 있습니다: `AI 스킬`, `HTML 앱`, `스킬 + 앱 동시에`.
- `스킬 + 앱 동시에`를 선택하면 한 번의 대화로 AI 스킬과 HTML 앱이 함께 만들어집니다. 앱은 내 스킬 목록에 바로 추가되고(이름 뒤 "(앱)" 표시), 스킬은 확인 화면을 거쳐 저장됩니다.
- HTML 앱만 선택하면 양식 첨부 단계 없이 세 가지 질문(필요한 앱 → 기능·화면 → 디자인)만으로 앱이 만들어집니다.
- 대화로 만든 스킬의 품질을 높였습니다. 이전에는 입력 필드를 만들어도 실행 시 입력값이 AI에게 전달되지 않는 경우가 있었는데, 이제 모든 입력값이 지시문에 자리표시자(`{{field_0}}` 형태)로 반드시 연결되도록 설계 규칙을 강화했습니다. 역할·작업·출력 형식을 충실히 담은 지시문과 필수 입력 여부도 함께 설계됩니다.

---

## EduNote v1.13.2

### 화면·사용성 개선
- 설정 화면의 `파일 저장 폴더` 카드와 사용 방법 화면의 단계 번호가 다크 모드에서도 올바른 색으로 보이도록 수정했습니다.
- 화면 곳곳의 알림창(시스템 팝업)을 화면 우측 토스트 알림으로 교체했습니다. 오류·주의·완료 상황이 작업 흐름을 막지 않고 부드럽게 표시되며, 색상으로 종류를 구분합니다.
- Google AI Studio에서 API 키 발급이 거부되고 "Google Cloud에서 만들기" 안내가 나오는 경우의 대응 방법(개인 계정 재시도 → Cloud Console 직접 발급 → 결제 요구 시 주의사항)을 설정 화면 발급 가이드·온보딩·사용 방법 화면·사용자매뉴얼에 추가했습니다.

### 보안·안정성 개선
- 폴더 열기 기능이 홈 폴더·문서·다운로드·앱 데이터 등 허용된 위치의 실제 폴더만 열도록 경로 검증을 강화했습니다.
- AI 생성 요청의 형식·크기를 앱 내부에서 한 번 더 검증해 비정상 요청이 전송되지 않도록 했습니다. (20MB 초과 첨부, 빈 요청 등은 친절한 안내와 함께 차단)
- 생성 결과 버전 기록(이전 결과 다시 보기)을 화면에 표시하기 직전에 한 번 더 소독(sanitize)해, 저장된 기록이 변조되더라도 악성 스크립트가 실행되지 않도록 했습니다.
- 설정 저장과 백업 복원 시 항목별 형식(켜기/끄기 값, 경로, 문자열)을 검증해, 손상되거나 변조된 백업 파일이 설정을 망가뜨리지 않도록 했습니다.
- HTML 미리보기·PDF 변환에 쓰이는 임시 파일을 실행할 때마다 새로 만드는 예측 불가능한 임시 폴더에 저장하고, 앱 종료 시 자동으로 정리하도록 했습니다.
- 나라장터 인증키·인터넷 가격조회 Client 정보를 화면 쪽에서 주고받지 않고 앱 내부 저장소에서만 읽도록 바꿔, 저장한 Client Secret이 화면에 다시 노출되지 않도록 했습니다. (입력칸을 비워 두고 저장해도 기존 Secret은 유지됩니다)
- 링크 미리보기·이미지·스크린샷 등 인터넷 조회 기능이 이 컴퓨터의 내부 주소(localhost 등)로는 접근하지 않도록 차단했습니다. 학교 인트라넷 주소 조회는 그대로 사용할 수 있습니다.
- 앱 화면(메인 창·데모 창)에 렌더러 샌드박스를 적용하고, 나의 수업 자료실의 내장 브라우저가 http(s) 주소만 표시하며 새 창 요청은 기본 브라우저로만 열리도록 강화했습니다.
- 화면 저장소(localStorage) 용량이 가득 차도 앱이 멈추지 않도록 저장 실패를 안전하게 처리했습니다. (생성 기록·자료실·뽑기 기록·메모 등)
- AI 생성 실패 시 원인(API 키 없음/키 오류/사용량 초과/시간 초과/네트워크 오류)을 구분해 안내하도록 개선했습니다. 이전에는 원인과 관계없이 같은 사용량 안내만 표시되었습니다.
- 생성 중단 버튼이 해당 화면의 생성만 멈추도록 고쳤습니다. 이전에는 한 화면에서 중단하면 다른 화면에서 진행 중이던 생성도 함께 끊겼습니다. 중단 직후 뒤늦게 도착한 결과가 화면에 반영되는 문제도 함께 고쳤습니다.

### AI 생성 모델 자동 전환 재도입 (무료 키 사용 환경 개선)
- 이전에 도입했다가 동작 문제로 제거했던 모델 자동 전환을, 모델 이름을 추측하지 않고 **API 키에서 실제 사용 가능한 모델을 조회해 검증된 목록으로만 전환**하는 방식으로 다시 도입했습니다.
- 무료 키의 분당 요청 제한에 걸리면 Google이 알려주는 대기 시간만큼 자동으로 기다렸다가 **같은 모델로 한 번 더 시도**합니다. 일괄 생성 중 잠깐의 제한으로 전체가 실패하던 문제가 줄어들고, 생성 결과의 문체도 일정하게 유지됩니다.
- 하루 무료 한도를 모두 사용한 경우에는 막연한 사용량 안내 대신 "일일 한도 소진" 안내를 표시합니다.
- 모델 목록 조회에 실패하면 기존처럼 기본 모델 1개로만 동작하므로, 어떤 경우에도 이전보다 나빠지지 않습니다.

### AI 생성 품질·효율 개선
- 무료 키 사용 시 AI 호출 사이에 자동으로 간격을 두어, 학생 일괄 생성 중 분당 요청 제한(15회/분)에 걸려 중간에 실패하던 문제를 줄였습니다.
- 텍스트형 생성(학생기록·질문답변·업무기록)에 출력량 상한을 두어 비정상적으로 긴 출력으로 인한 토큰 낭비를 막았습니다.
- 예산안·퀴즈·슬라이드·NEIS 분석 등 표 형식 데이터를 만드는 기능에 JSON 전용 응답 모드를 적용해, "응답을 해석하지 못했습니다" 오류가 줄었습니다.
- 학생기록 생성 결과에 교외 대회·수상, 공인어학시험, 특정 대학명 등 생활기록부에 기재할 수 없는 표현이 섞이면 자동으로 감지해 알림으로 안내합니다.
- 학생 일괄 생성 시 학생마다 서술 시작 관점을 바꿔, 같은 특성을 입력한 학생들의 문장이 비슷한 구조로 반복되는 것을 줄였습니다.
- 교과 세특 작성 지침의 흐름 기준을 하나로 통일하고 성취수준(상/중/하)별 표현 기준을 명시했으며, 창의적 체험활동은 활동 영역(자율/동아리/진로/봉사)별 기재 기준을 구분해 적용합니다.
- 문서작성기가 입력에 없는 법령명·공문번호를 추측해 쓰지 않도록 하고, 회의록 표 구조를 고정 템플릿으로 제공해 표가 깨지는 문제를 줄였습니다.
- 기재요령 챗봇의 대화 기록 전송을 최근 6개로 제한하고 챗봇 안내문을 경량화해 무료 키 사용량을 절약했습니다.
- 문서작성기에서 문서가 만들어지는 동안 생성 중인 내용을 실시간 미리보기로 보여줍니다. 긴 문서도 진행 상황을 눈으로 확인할 수 있습니다.
- 워크시트 생성 직후 A4 분량을 추정해, 한 장을 넘을 것 같으면 미리 안내합니다. (인쇄해 본 뒤에야 알게 되던 문제 예방)
- 텍스트 형식 첨부 파일(txt·md·csv)을 더 효율적인 방식으로 전송해 무료 키 사용량을 절약하고, 지나치게 긴 문서는 앞부분만 사용하도록 했습니다.

---

## EduNote v1.13.1

### 문서작성기 메뉴명 변경 및 표 작성 기준 개선
- '공문서 작성기' 메뉴명을 '문서작성기'로 변경했습니다.
- 상세 공문 작성 시 항상 표가 들어가던 것을, 세부추진계획(일정·역할 분담 등 줄 단위로 비교·정리해야 하는 내용)이 있을 때만 그 부분에 한해 표를 사용하도록 개선했습니다.

### 생성 프롬프트 기준 복원
- 공문서 작성기, 수업자료 AI, 생기부도우미의 결과물 작성 프롬프트를 v1.12.1 기준으로 복원했습니다.
- v1.13.0에서 추가했던 학생기록 종결어미 후처리, 공문서 A4 페이지 분할 지시, 수업자료 인쇄 분할 보강 지시를 제거해 이전 버전의 생성 흐름과 문체로 돌아갔습니다.
- UI 색상/폰트 리뉴얼, 온보딩, 메뉴 구조, 백업 안내 등 v1.13.0의 화면 개선은 유지됩니다.

### 내 스킬 만들기 개선
- 프롬프트 진단 기능을 제거했습니다. 작성한 프롬프트를 점수나 이슈 목록으로 평가하지 않습니다.
- `AI 도움받기`는 현재 작성된 프롬프트를 바탕으로 바로 개선안을 만들어 프롬프트 입력칸에 반영합니다.
- AI가 도구 설명, 입력 변수, 기존 프롬프트를 함께 보고 도구의 실제 의도와 결과물 장르를 먼저 파악한 뒤 개선하도록 지시를 강화했습니다.
- 일기 분석, 감상문 피드백, 상담 기록, 문서 요약, 공문서 작성, 수업자료 제작, 학생 기록 작성 등을 서로 혼동하지 않도록 하고, 일기 분석 도구를 공문서 형식으로 바꾸는 식의 장르 전환을 금지했습니다.

### 설치 방법
1. Assets에서 `edunote_1.13.1_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.

## EduNote v1.13.0

### UI 색상/폰트 리뉴얼 (따뜻한 톤 팔레트 적용)
- 앱 전체의 배경·테두리·텍스트 색상을 기존의 차가운 회색 톤에서 베이지·차콜 계열의 따뜻한 톤 팔레트로 새로 단장했습니다. 라이트·다크 모드 모두 적용되며, 화면 구성과 사용 방법은 그대로입니다.
- 기본 글꼴을 Pretendard로 적용해 한글 가독성을 높였습니다.
- 교무행정(에메랄드)·수업자료(앰버)·학생기록(인디고)·AI스킬즈(핑크) 등 영역별 강조 색상은 기존과 동일하게 유지하여 화면 간 구분은 그대로 유지됩니다.

### 온보딩과 개인정보 안내 개선
- EduNote 실행 시 사용 동의 온보딩이 먼저 표시되도록 했습니다. 저장된 이름·소속기관·학교급·담당 학년/반 정보는 기존 값이 있으면 그대로 채워집니다.
- 사용 동의 단계에서 체크 후 바로 앱을 시작할 수 있는 `동의하고 창 닫기` 버튼을 추가했습니다.
- 저장된 API 키가 있을 때는 키 원문을 다시 보여주지 않고, 저장된 키가 있다는 안내만 표시합니다.
- 도움말 / 정보와 사용 방법 화면에 로컬 저장, 백업 범위, API 키 백업 제외, Gemini API 사용 시 개인정보 주의사항을 안내했습니다.

### 메뉴와 진행 상태 개선
- 공문서, 학생기록, 우리반기록, 수업 도구, 내 스킬 등 하위 메뉴를 상단 가로 탭에서 선택하도록 정리해 사이드바 중복 메뉴를 줄였습니다.
- 다른 메뉴로 이동해도 생성 중인 작업의 진행률이 상단 탭 또는 접힌 사이드바 아이콘에 표시되도록 개선했습니다.
- 왼쪽 사이드바를 접어도 홈, 설정, 교무행정AI, 공문요약, 공문서 작성기, 수업자료AI, 학생기록AI, AI 스킬즈, 사용 방법, 도움말 / 정보 아이콘이 남아 바로 이동할 수 있습니다.
- 홈 화면의 마지막 백업 카드 옆에 `자료 백업하기` 버튼을 추가해 설정 화면으로 들어가지 않아도 바로 백업할 수 있습니다.
- 사이드바 제목 바와 본문 상단 탭·헤더 높이를 맞춰 화면 전환 시 상단 단차가 생기지 않도록 정리했습니다.
- 홈 화면의 마지막 백업 시간과 `자료 백업하기` 버튼이 좁은 창에서 겹치지 않도록 표시 방식을 개선했습니다.

### 생성 결과 품질 개선
- 학생기록 계열 결과에서 학생 이름이 본문에 반복적으로 들어가지 않도록 후처리를 강화했습니다.
- 행동발달, 교과 세특, 스포츠클럽, 창체 등 학생 서술 문장이 `노력함.`, `성장함.`처럼 생활기록부 문체에 맞게 끝나도록 보강했습니다.
- 공문서 작성기는 예시 문서 형식에 더 가깝게 작성하고, A4 기준으로 문서가 길어질 때 문장·표·항목이 중간에 잘리지 않는 지점에서 다음 장으로 넘어가도록 지침을 강화했습니다.
- 수업자료는 특별한 요청이 없으면 A4 한 장 기준으로 구성하고, 내용이 길어질 때 문항·표·활동 단위가 중간에 잘리지 않도록 인쇄 규칙을 보강했습니다.

### 공문요약 캘린더 개선
- 공문요약·업무추출 결과를 Google Calendar에 추가할 때, 일반 일정 날짜보다 `마감`, `기한`, `제출`, `신청`, `접수`, `회신` 문맥의 날짜를 우선 사용하도록 개선했습니다.
- Google Calendar 상세 설명에 마감일 기준으로 만든 일정이라는 안내를 추가했습니다.

### 시연 샘플 개선
- Demo 창과 시연용 샘플 문서의 공문요약·업무추출 예시에 참고 웹사이트 주소를 추가했습니다.

## EduNote v1.12.2

### 전체 자료 백업 범위 확대
- 그동안 백업에서 빠지던 **공문 생성 히스토리, 사이드 메뉴 순서, 공문서 양식 즐겨찾기, 학생별 생성 히스토리**가 이제 전체 자료 백업에 함께 저장됩니다.
- 백업을 불러오면 위 자료까지 복원되며, 복원 직후 화면이 자동으로 새로고침되어 바로 반영됩니다.
- 이전 버전(공문 히스토리 등이 없던 형식)의 백업 파일도 오류 없이 그대로 불러올 수 있습니다.

### AI 생성 안정성 개선 (모델 자동 폴백)
- AI 결과 생성에 사용하는 모델이 요청 제한(쿼터)이나 접근 불가로 막히면, 자동으로 **다음 대체 모델로 이어서 생성**을 시도하도록 개선했습니다.
- 무료/유료 각각 3개 모델을 순서대로 시도하므로, 특정 모델이 일시적으로 막혀도 결과 생성이 한 번에 전부 중단되지 않습니다.

### 접근성 개선
- 안내 팝업(API 키 안내, 학교급 선택)을 **Esc 키로 닫을 수 있게** 했습니다.
- 팝업과 아이콘 버튼(다크모드 전환, 메뉴 접기·펼치기 등)에 화면 낭독기(스크린리더)용 설명을 추가해 키보드·보조기기 사용성을 높였습니다.

### 내부 품질 개선
- 핵심 로직(개인정보 마스킹, 가져온 도구 검증, 생성 결과 HTML 정화 등)에 대한 자동 테스트를 도입해 향후 변경 시 기능이 깨지는지 자동으로 검증할 수 있게 했습니다. (사용 화면에는 변화가 없습니다.)

## EduNote v1.12.1

### 예산안작성 품목 정확도 개선
- 예산 제목과 관련 없는 일반 품목(볼펜, 복사용지, 커피 등)이 자동으로 끼워지던 문제를 개선했습니다. 제목·희망물품에 어울리는 품목이 있으면 그 품목만 후보로 쓰고, 아무것도 안 잡힐 때만 일반 카탈로그를 사용합니다.
- Gemini에게 제목 주제를 먼저 분석해 관련 품목만 생성하도록 지시를 강화했습니다.
- 과목별(비율) 방식으로 만들 때도 교육운영비·일반운영비·업무추진비 세 과목 모두에 예산 제목 주제와 연결된 품목이 들어가도록 했습니다. (주제와 연결이 어려운 과목만 일반 운영성 품목 사용)

### HTML 앱 만들기 개선
- 어휘 플래시카드 예시를 바꿨습니다. 예시를 클릭하면 사과/apple, 책/book, 학교/school 등 6쌍이 카드로 미리 채워진 채 앱이 만들어집니다. 단어/뜻 쌍을 직접 추가·수정할 수 있고, 앞면과 뒷면 뒤집기·맞힘/틀림·정답률도 동작합니다.
- 앱 생성이 실패했을 때 "다시 시도해 주세요" 대신 실제 원인(API 키 미설정, 쿼터 초과, 네트워크 시간 초과 등)을 그대로 표시하도록 개선했습니다. API 일시 제한 중에는 생성 버튼이 비활성화됩니다.

### 양식 인쇄 추가·개선
- **학급 회의록**: 일시·사회·서기·참석 현황 표와 지난 실천 결과·안건·협의 내용·결정 사항·건의사항 섹션을 포함한 A4 양식입니다.
- **1인 1역할**: 역할과 담당 학생을 한 줄씩 입력하면 표로 만들어 인쇄할 수 있습니다.
- **연수 등록부**: 참가자 명단을 번호별 칸으로 입력하고 「명단 추가」 버튼으로 20명을 넘겨 계속 추가할 수 있게 했습니다. 명단이 길면 다음 장으로 이어져 인쇄됩니다.

### AI 스킬즈 — 스킬 만들기 고도화

- **프롬프트 진단**: 작성한 프롬프트의 약점을 AI가 분석해 점수·한 줄 총평과 함께 이슈별 개선 제안을 보여줍니다. 이슈는 심각도(높음/중간/낮음)에 따라 색상으로 구분되며, "개선안 적용" 버튼으로 전체 프롬프트를 한 번에 교체할 수 있습니다. 기존 "AI 도움받기"와 나란히 제공됩니다.
- **테스트 실행**: 스킬 만들기 4단계(신규)에서 저장 전에 샘플 값을 직접 입력해 AI 결과를 미리 확인할 수 있습니다. 프롬프트를 수정하고 바로 다시 돌려볼 수 있어 프롬프트 튜닝이 쉬워집니다.

### 설치 방법
1. Assets에서 `edunote_1.12.1_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.12.0

### 예산안작성 개선
- 예산안을 만들기 전에 `과목별 비율` 방식과 `일반 작성` 방식을 선택할 수 있게 했습니다.
- 일반 작성 방식에서는 예산 과목 없이 품목 중심으로 예산안을 작성할 수 있고, 예산 과목 칸은 비워서 보여줍니다.
- 예산 제목, 예산 금액, 구입 물품 입력칸은 실제 값 대신 회색 예시 문구로 안내합니다.
- 저장한 예산안과 입력값이 앱을 다시 실행해도 유지되도록 했고, 저장된 계획 목록에 저장 날짜와 시간을 표시합니다.
- 예산안 생성이 완료되면 기존 생성 완료 효과음과 같은 소리가 나도록 했습니다.
- 최소·최대 수량은 필요한 경우에만 입력하면 된다는 안내를 예산안 표 위에 추가했습니다.

### 사이드 패널 접기 개선
- 메인 사이드바와 각 기능 화면의 하위 사이드 패널 접기 버튼 모양을 통일했습니다.
- 공문 요약, 공문서 작성기, 양식 인쇄, 수업자료 생성, 내 도구 실행 화면의 입력 패널을 접고 펼칠 수 있게 했습니다.
- 행동발달, 스포츠클럽, 교과 세특, 창체 화면의 학생 목록 패널도 접고 펼칠 수 있게 했습니다.

### 설치 방법
1. Assets에서 `edunote_1.12.0_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.11.5

### 예산안작성 트리 구조 개편
- 교육운영비·일반운영비·업무추진비 과목을 트리의 1단계 항목으로 두었습니다. 모두 접으면 과목 세 줄만 보이고, 펼치면 '과목 > 품목' 2단계로 보이며 필요하면 3단계까지 만들 수 있습니다.
- 자동으로 생기던 '세부' 단계를 없앴습니다.
- 과목·상위 항목 행은 규격·단가·수량 칸을 비우고, 소계만 하위 합계로 표시합니다.
- '모두 접기/펼치기'와 '과목별 정렬' 버튼을 추가했습니다.
- 산만하던 행 추가 방식을 정리해, 각 과목 행의 '+ 품목' 버튼으로 품목을 추가하도록 했습니다. (품목 행에는 세부 항목 추가 아이콘만 둡니다.)
- 인터넷 가격 조회로 추가한 품목이 해당 과목 맨 위에 들어가도록 했습니다.
- 인터넷 가격 조회 결과에 마우스를 올리면 상품 이미지를 미리보기로 보여줍니다.
- CSV로 내려받을 때 앱 화면에서 보이는 예산안 모양(과목·들여쓰기·상위 행 빈칸)과 똑같이 저장되도록 했습니다.

### 설치 방법
1. Assets에서 `edunote_1.11.5_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.11.4

### 예산안작성 개선 (메뉴명 변경)
- '예산사용계획' 메뉴 이름을 '예산안작성'으로 바꿨습니다.
- 상단의 중복 '예산안 만들기' 버튼을 없애고, 왼쪽 입력 영역의 버튼 하나로 예산안 틀 생성과 품목 자동 생성을 한 번에 하도록 통합했습니다.
- 과목별 비율을 입력하지 않아도 예산안을 만들 수 있게 했고, 비율이 비어 있으면 업무추진비를 총 예산의 3%로 두고 나머지를 자동 배분합니다.
- 자동 생성·단가 추정과 내장 품목의 단가를 가능하면 만원 단위로 정리했습니다.
- '0원 맞추기'를 직접 수정한 행은 그대로 두고 나머지 수량을 최소한만 조정하는 방식으로 개선했습니다. 품목별 최소·최대 수량을 정하면 그 범위 안에서만 조정합니다.
- 하위 항목을 가진 부모 행의 규격에 표시되던 '묶음' 문구를 없앴습니다.
- '인터넷 가격 조회로 품목 추가' 검색 패널을 새로 넣었습니다. 키워드로 검색해 실제 상품과 단가를 확인하고, 누르면 선택한 과목에 품목으로 바로 추가됩니다. (나라장터·인터넷 가격조회 키 입력 시)

### 공문서 작성기 개선
- 미리보기와 생성 결과, 저장 문서(PDF·HTML·Word)에서 한글 제목과 본문이 글자 중간에서 어색하게 줄바꿈되지 않도록 어절 단위 줄바꿈을 적용했습니다.
- 계획서 예시에서 폐지된 'AI 디지털교과서' 관련 내용을 제거했습니다.

### 설치 방법
1. Assets에서 `edunote_1.11.4_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.

---

## EduNote v1.11.3

### 예산사용계획 개선
- 예산 과목을 교육운영비, 일반운영비, 업무추진비 기준으로 정리했습니다.
- 예산안 표를 접고 펼칠 수 있는 트리 형태로 단순화했습니다.
- 하위 항목은 같은 과목 계열의 연한 색으로 구분되도록 개선했습니다.
- 예산 초안 예시를 디지털선도학교 운영 예산 기준으로 바로 확인할 수 있게 했습니다.
- 예산안 만들기 버튼을 입력 영역 아래에 더 잘 보이도록 배치했습니다.
- 배정예산, 집행예산, 남은예산을 예산안 위쪽에서 더 잘 보이게 개선했습니다.
- 0원 맞추기 버튼이 더 눈에 잘 띄도록 개선했습니다.
- 0원 맞추기에서 초과와 부족 금액을 모두 줄일 수 있도록 수량 조정 방식을 개선했습니다.
- 예산안 표에서 단가 출처 문구를 없애고 화면을 더 단순하게 정리했습니다.
- 두 번째 단계 항목에도 하위 항목을 바로 추가할 수 있게 했습니다.
- 단가 입력칸의 위아래 조정 단위를 1,000원으로 변경했습니다.
- AI 예산안 만들기에서 학교회계 과목 성격을 더 잘 반영하도록 개선했습니다.
- 상품권류는 자동 생성 항목에서 제외하고, 학생 지원 항목은 간식과 기념품 중심으로 생성하도록 했습니다.
- CSV 저장과 불러오기에서 3단계 예산 구조가 유지되도록 개선했습니다.
- 예산 관련 질문을 할 수 있는 NotebookLM 연결 버튼을 추가했습니다.

### 시작 안내 개선
- Gemini API 키가 없을 때 앱 사용이 제한될 수 있음을 시작 화면에서 안내하도록 했습니다.
- API 키 발급 방법과 설정 화면으로 바로 이동할 수 있게 했습니다.

### 설치 방법
1. Assets에서 `edunote_1.11.3_portable.exe` 파일을 내려받습니다.
2. 내려받은 EXE 파일을 실행합니다. 설치가 필요 없는 portable 버전입니다.
3. 기존 설정과 저장 데이터는 기존 데이터 폴더를 기준으로 그대로 사용됩니다.
