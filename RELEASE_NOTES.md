### 설치 방법
1. 아래 Assets에서 `edunote_*.exe` 파일을 다운로드합니다.
2. 다운로드한 EXE 파일을 실행합니다. (설치 불필요)
3. 설정에서 Gemini API 키를 입력하면 바로 사용 가능합니다.

### Gemini API 키 발급
[aistudio.google.com](https://aistudio.google.com) 에서 무료로 발급받을 수 있습니다.

---

## ✨ v1.3.3 업데이트 내역

### 나만의 자료실 — 분류 즉시 변경 기능
- **분류 태그 클릭으로 분류 변경**: 자료 카드의 분류 태그를 클릭하면 드롭다운 메뉴가 나타나 다른 카테고리로 즉시 이동 가능
- **미분류 자료에 분류 추가**: 분류가 없는 자료에는 `+ 분류` 버튼이 표시되어 클릭하여 분류 지정 가능
- 외부 클릭 시 드롭다운 자동 닫힘

### API 키 등록 상태 시각화 개선
- **사이드바 하단 표시**: 키가 등록되어 있으면 사이드바 하단에 초록색 ✓ "API 키 설정 완료" 표시
- **설정 화면 표시**: "새 API 키" 입력란 라벨 옆에 초록 배지 "✓ API 키 설정 완료" 강조 표시

---

## ✨ v1.3.2 업데이트 내역

### API 키 검증 로직 대폭 개선
- **모든 모델 순차 시도**: 첫 모델에서 403이 발생해도 즉시 종료하지 않고 다음 모델까지 시도
  - 새 계정/프로젝트에서 특정 모델만 임시 차단된 경우, 다른 모델이 작동하면 키 유효 판정
  - 검증 모델 확장: `gemini-2.5-flash` → `2.0-flash` → `1.5-flash` → `1.5-flash-8b` (총 4개)
- **에러 원인 자동 분류**: 모든 모델이 403일 때 원본 메시지를 분석하여 정확한 안내 제공
  - **GCP API 미활성화** (`"has not been used"`, `"not enabled"`): "1~2분 후 재시도" + "키 재발급" 안내
  - **학교/조직 Workspace 차단** (`"workspace"`, `"organization"`, `"policy"`): 개인 Gmail 키 발급 안내
  - **기타 403**: 가능한 원인 3가지를 모두 안내
- **원본 에러 메시지 노출**: 사용자가 정확한 원인을 직접 확인할 수 있도록 Google 에러 메시지 표시

### 상위 모델 최우선 폴백 정책 유지
- `generateContent` / `generateContentMultipart`는 기존 v1.3.1 스마트 폴백 그대로 유지
  - 매 호출마다 사용 가능한 최상위 모델부터 시도
  - 쿼터 초과 시 60초 임시 차단 → 자동 복귀
  - 권한 거부는 영구 차단

---

## ✨ v1.3.1 업데이트 내역

### Gemini 모델 스마트 폴백 (핵심 개선)
- **자동 최상위 모델 선택**: 계정별로 사용 가능한 가장 좋은 모델을 자동 선택
  - 유료 계정: `gemini-2.5-pro` 우선 사용
  - 무료 Gmail 계정: `gemini-2.5-flash`부터 자동 사용 (2.5-pro는 403 차단 후 자동 폴백)
- **쿼터 초과 시 임시 폴백 + 자동 복귀**: 쿼터에 걸린 모델만 60초간 차단하고 차순위 모델로 자동 전환
  → 60초 경과 시 자동으로 상위 모델로 복귀 (영구 고정 안 됨)
- **권한 거부는 영구 차단**: 403/400/404로 막힌 모델은 재시도하지 않아 불필요한 호출 제거

### API 키 인식 오류 개선
- **학교 Workspace 계정 안내 추가**: 403 PERMISSION_DENIED 오류를 "키 잘못됨"이 아닌 "조직 정책 차단"으로 정확히 구분
  - 학교/기관 Google Workspace 계정 사용자에게는 "개인 Gmail로 새 키 발급" 안내 메시지 표시
- **API 키 저장 시 자동 테스트**: 저장 버튼 클릭 시 자동으로 키 유효성을 확인한 후 저장 (실패 시 저장 차단)
- **쿼터 한도 도달 시 메시지 개선**: "키 정상 — 바로 사용 가능합니다" 안내로 혼란 제거

### UI 개선
- **사이드바 "API 키를 설정해 주세요" 배너 클릭 가능**: 배너를 클릭하면 바로 설정 화면으로 이동
- **수업자료AI 색상 변경**: 주황색(amber) → 보라색(violet)으로 변경하여 사이드바 다른 메뉴와 시각적 구분 향상

---

## ✨ v1.3.0 업데이트 내역

### API 키 인식 오류 수정 (중요)
- **키 인식 오류 버그 수정**: 유효한 API 키임에도 "인식하지 못했습니다"로 표시되던 문제 해결
  - Google API는 잘못된 모델명과 잘못된 키 모두 동일한 400 오류를 반환하는데, 기존 코드가 이를 구분하지 못하고 항상 "키 오류"로 처리했던 문제
  - 이제 모델명 오류일 경우 자동으로 다음 모델로 전환 시도 (gemini-2.5-flash → 2.0-flash → 1.5-flash)

### 창체 특기사항 — 기능 대폭 확장
- **파일 입력 확장**: 이미지 외에 **PDF**, **HWPX** 파일에서 연간 지도 계획 자동 추출 지원
  - PDF: Gemini AI가 직접 문서 분석
  - HWPX: 텍스트 직접 추출 후 분석
  - HWP(구형): HWPX 또는 PDF 변환 안내
- **태그 대폭 확장**: 임원·역할, 주요 활동, 역량·태도 각 **30개씩 추가** (기존 대비 약 2배)
  - 자율활동·동아리활동에 어울리는 항목 중심으로 구성
  - 전체 가나다순 정렬
- **진행률 표시 수정**: 일괄 생성 시 상단 진행률 배너가 표시되지 않던 문제 해결

### 스포츠클럽 특기사항
- 긍정·부정 특성 태그 각 30개 추가 및 선택 UI 개선
- 일괄 생성 진행률 표시 수정

### 수업자료 AI
- **게임 제작 추가**: 수업 주제 기반 교육용 미니 게임 HTML 자동 생성
- 수업자료 전용 로딩 메시지 적용

### 나만의 자료실
- **구글/유튜브 검색 통합**: 검색 결과를 앱 내에서 바로 보고 자료실에 추가 가능

### 결과 편집기
- **Ctrl+Z 실행 취소 지원**: 생성 결과 교체 시 브라우저 실행 취소 스택에 기록되어 Ctrl+Z로 이전 결과 복원 가능

---

## ✨ v1.2.1 업데이트 내역

### 교무행정 AI — 문서 품질 개선
- **보고서 구조 차별화**: 계획서와 동일한 구조에서 벗어나 결과 보고서 고유 형식으로 개편
  - 추진 개요(요약) → 추진 실적(계획 vs 결과 비교표) → 세부 운영 결과(사진 첨부 포함) → 만족도 조사 결과 → 예산 정산(계획액/집행액/잔액) → 운영 성과 및 제언
  - 완료형 문체(`~하였음.`, `~완료함.`) 자동 적용, 미래형 표현 제거
- **문자 메뉴 이름 변경**: '문자메세지 작성' → '문자&소통메세지'
- **문자 예시 줄바꿈 수정**: 예시 문서 내 줄바꿈이 올바르게 표시되도록 개선

---

## ✨ v1.2.0 업데이트 내역

### 교무행정 AI — 문서 품질 개선
- **보고서 구조 차별화**: 계획서와 동일한 구조에서 벗어나 결과 보고서 고유 형식으로 개편
  - 추진 개요(요약) → 추진 실적(계획 vs 결과 비교표) → 세부 운영 결과(사진 첨부 포함) → 만족도 조사 결과 → 예산 정산(계획액/집행액/잔액) → 운영 성과 및 제언
  - 완료형 문체(`~하였음.`, `~완료함.`) 자동 적용, 미래형 표현 제거
- **문자 메뉴 이름 변경**: '문자메세지 작성' → '문자&소통메세지'
- **문자 예시 줄바꿈 수정**: 예시 문서 내 줄바꿈이 올바르게 표시되도록 개선

### 수업 슬라이드 — 이미지 생성 개선
- **슬라이드별 이미지 생성 버튼**: 각 슬라이드 카드에 "이미지 생성" 버튼 추가 — 원하는 슬라이드만 선택적으로 생성
- **전체 이미지 생성 + 진행률 표시**: 헤더의 "전체 이미지 생성" 버튼으로 일괄 생성, X/N (YY%) 진행률 실시간 표시
- **일관된 그림체**: 같은 슬라이드 세트 내 모든 이미지에 동일한 스타일 프리픽스 적용
- **이미지 재생성**: 생성된 이미지 위에 마우스를 올리면 재생성 버튼 표시
- **이미지 생성 모델 수정**: 최신 SDK API 및 모델명으로 업데이트

### 수업자료 — 직접 편집 기능 추가
- **슬라이드 인라인 편집**: 슬라이드 제목·글머리기호·교사 메모를 카드에서 직접 클릭하여 수정
- **워크시트 편집**: 생성된 워크시트를 문서 편집기에서 직접 수정 후 PDF·Doc·MD 저장
- **퀴즈 HTML 편집**: "HTML 편집" 버튼으로 퀴즈 소스 코드 직접 수정, "적용" 시 미리보기 갱신

### 퀴즈 앱 개선
- **시작 화면 추가**: "퀴즈 시작" 버튼 — 버튼 누르기 전까지 문제 미표시
- **즉각적인 정답 피드백**: 답 선택 즉시 정답/오답 색상 표시
- **결과 화면**: 최종 점수 + 전체 문항 정답 목록 + "다시 풀기" 버튼

### 탭 전환 시 내용 유지
- 슬라이드/워크시트/퀴즈/수업계획서 탭을 자유롭게 이동해도 각 탭의 생성 결과가 유지됨
- 재생성 시에는 해당 탭 결과만 초기화

### 앱 아이콘 교체
- 새 아이콘 디자인으로 교체

---

## ✨ v1.1.0 업데이트 내역

### 수업 슬라이드 — 이미지 자동 생성
- **나노바나나 2(Gemini 이미지 생성 모델) 연동**: 슬라이드 생성 시 각 슬라이드 내용에 맞는 이미지를 AI가 자동으로 생성·삽입합니다. 별도 API 키 불필요, 기존 Gemini 키 재사용
- **슬라이드 텍스트 최적화**: 글머리표 2~3개 · 20자 이내로 간결하게 구성 — 교실 뒷자리 학생도 한눈에 읽을 수 있습니다
- **프레젠테이션 모드 가독성 대폭 향상**: 제목 및 내용 폰트를 크게 확대 (교실 프로젝터 환경 최적화)
- 키보드 방향키(←/→)로 슬라이드 이동, ESC로 종료

### 수업 워크시트
- **학년별 폰트 크기 자동 적용**: 초등 12pt / 중학 11pt / 고등 10pt
- **A4 1장 최적화**: 모든 활동이 A4 1장 안에 들어가도록 레이아웃 개선
- 명칭 변경: 활동지 → 워크시트

### 설정
- **API 키 삭제 기능 추가**: 설정 화면에서 저장된 Gemini API 키를 삭제할 수 있습니다

### 기타
- 앱 시작 시 주의사항 문구 개선
